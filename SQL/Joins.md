# Joins

- Primary key uniquely identifies a row in a table
- Foreign key estblishes reationship with another table's primary key

### Inner Join
- Default join in sql
- inner join == join

### Outer Join
- Outer Join = Full Join

Unlike union, union all doesn't ignore duplicates
